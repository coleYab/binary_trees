#include "binary_trees.h"

size_t binary_tree_leaves(const binary_tree_t *tree)
{
    size_t l_right = 0, l_left = 0;

    if (!tree)
        return (0);

    if (tree->left || tree->right)
    {
        return (count())
    }

   return (1);
}